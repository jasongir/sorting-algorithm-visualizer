import React from "react";
import Button from "@material-ui/core/Button";

// and then put these in a buttongroup
const FrameControlBtn = ({ icon, handleFrameControl }) => {
	return <Button></Button>;
};

export default FrameControlBtn;
